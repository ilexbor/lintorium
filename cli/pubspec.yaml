name: lintorium_cli

version: '1.0.0'

license: 'BSD-3-Clause'

repository: https://github.com/ilexbor/lintorium

issue_tracker: https://github.com/ilexbor/lintorium/issues

dependencies:
  collection: ^1.18.0
  interact: ^2.2.0
  version: ^3.0.2
  yaml: ^3.1.2

dev_dependencies:
  lintorium:
    path: '..'

environment:
  sdk: '>=3.0.0 <4.0.0'

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

topics:
  - analysis-options
  - analyzer-diagnostics
  - code-style
  - linter-rules
  - lints

publish_to: none